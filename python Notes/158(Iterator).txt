			========================
				Iterators in Python                          
			========================
------------------------------------------------------
Why should WE use Iterators:
------------------------------------------------------
=>In modern days, we have a lot of data in our hands, and handling this huge amount of data creates problems for everyone who wants to do some sort of analysis with that data.So, If you’ve ever struggled with handling huge amounts of data, and your machine running out of memory, then WE use the concept of Iterators in Python.
=>Therefore, Rather than putting all the data in the memory in one step, it would be better if we could work with it in bits or some small chunks, dealing with only that data that is required at that moment. As a result, this would reduce the load on our computer memory tremendously. And this is what exactly the iterators do.
=>Therefore, you can use Iterators to save a ton of memory, as Iterators don’t compute their items when they are generated, but only when they are called upon.
----------------------------------------------------------------------------------------------------------
=>Iterator in python is an object that is used to iterate over iterable objects like lists, tuples, dicts, str, and sets But not applicable on Non_iterable Objects like int,float,bool,complex and NoneType
=>The iterator object is initialized using the iter() method / Function. It uses the next() method for getting the value from that iteration.
=>Here iter() is used for converting Iterable object  into Iterator object.
=>next() is used for obtaining next element of iterator object and if no next element found in Iterator Object then we get an exception   called StopIteration.
=>On the object of Iterator, we can't perform Indexing and Slicing Operations bcoz They supply the value on demand .
--------------------------
Examples:
--------------------------
s = 'Python'
itobj = iter(s)
while True:
    try:
         item = next(s)      # Iterate by calling next
        print(item)
    except StopIteration:         # exception will happen when iteration will over
        break
======================================================
mytuple = ("apple", "banana", "cherry")
myit = iter(mytuple)
print(next(myit))
print(next(myit))
print(next(myit))
=======================================================

#IterEx1.py
import sys
x=[10,"Rossum","Python",34.56,True]
print("------------------------------------------------------")
print("Content of x =",x)
print("Type of x=",type(x))
print("------------------------------------------------------")
#Convert Iterable Object x into Iterator Object by using iter()
itrobj=iter(x)
print("Type of iterobj=",type(itrobj))
try:
	print(next(itrobj))
	print(next(itrobj))
	print(next(itrobj))
	print(next(itrobj))
	print(next(itrobj))
	print(next(itrobj))
except StopIteration:
	sys.exit()

#IterEx2.py
import sys
x=[10,"Rossum","Python",34.56,True]
print("------------------------------------------------------")
print("Content of x =",x)
print("Type of x=",type(x))
print("------------------------------------------------------")
#Convert Iterable Object x into Iterator Object by using iter()
itrobj=iter(x)
print("Type of iterobj=",type(itrobj))
while(True):
	try:
		print(next(itrobj))
	except StopIteration:
		break

#IterEx4.py
import sys
x=[10,"Rossum","Python",34.56,True]
print("------------------------------------------------------")
print("Content of x =",x)
print("Type of x=",type(x))
print("------------------------------------------------------")
#Convert Iterable Object x into Iterator Object by using iter()
itrobj=iter(x)
print("Type of iterobj=",type(itrobj))
for val in itrobj:
	print(val)

#IterEx5.py
import sys
x=(10,"Rossum","Python",34.56,True)
print("------------------------------------------------------")
print("Content of x =",x)
print("Type of x=",type(x))
print("------------------------------------------------------")
#Convert Iterable Object x into Iterator Object by using iter()
itrobj=iter(x)
print("Type of iterobj=",type(itrobj))
for val in itrobj:
	print(val)

#IterEx6.py
import sys
x={10,"Rossum","Python",34.56,True} # OR  frozenset({10,"Rossum","Python",34.56,True}
print("------------------------------------------------------")
print("Content of x =",x)
print("Type of x=",type(x))
print("------------------------------------------------------")
#Convert Iterable Object x into Iterator Object by using iter()
itrobj=iter(x)
print("Type of iterobj=",type(itrobj))
for val in itrobj:
	print(val)

#IterEx7.py
import sys
x={10:"Python",20:"Java",30:"Django"}
print("------------------------------------------------------")
print("Content of x =",x)
print("Type of x=",type(x))
print("------------------------------------------------------")
#Convert Iterable Object x into Iterator Object by using iter()
itrobj=iter(x)
print("Type of iterobj=",type(itrobj))
for val in itrobj:
	print("{}--->{}".format(val,x.get(val))) # OR print("{}--->{}".format(val,x[val]))

#IterEx8.py
import sys
x="PYTHON"
print("------------------------------------------------------")
print("Content of x =",x)
print("Type of x=",type(x))
print("------------------------------------------------------")
#Convert Iterable Object x into Iterator Object by using iter()
itrobj=iter(x)
print("Type of iterobj=",type(itrobj))
for val in itrobj:
	print(val)

#IterEx9.py
import sys
x=range(10,51,10)
print("------------------------------------------------------")
print("Content of x =",x)
print("Type of x=",type(x))
print("------------------------------------------------------")
#Convert Iterable Object x into Iterator Object by using iter()
itrobj=iter(x)
print("Type of iterobj=",type(itrobj))
for val in itrobj:
	print(val)

