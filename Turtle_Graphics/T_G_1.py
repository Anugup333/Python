import turtle
import time 

# turtle.left(45)   # that defines cursor gone it 45 degree left 
# turtle.right(90)   # that defines cursor gone it 90 degree right
# turtle.backward(100)  # that defies cursor gone in back
turtle.pendown()    # that defines down the pen in whatever direction says
turtle.penup()    # that defines up the pen in whatever direction says
turtle.forward(100)  # that defies cursor gone in straight

time.sleep(5)